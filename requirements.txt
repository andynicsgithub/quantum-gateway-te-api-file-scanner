# TE API Scanner Dependencies
# Minimum Python version: 3.9 (required for set[str], set[int] type annotations)

# HTTP client for TE API
requests>=2.31.0
urllib3>=2.0.0

# Windows support (conditional - only installed on Windows)
pywin32>=306; sys_platform == 'win32'

# File watching (watch mode)
watchdog>=3.0.0

# Encrypted zip archives (AES encryption)
pyzipper>=0.3.6
cryptography>=41.0.0

# SSH/SCP for AV fallback
paramiko>=3.4.0
